let inquirer,chalk;_092‍.x([["askForNoteContent",()=>askForNoteContent],["writeContent",()=>writeContent],["checkContent",()=>checkContent],["continueContent",()=>continueContent],["main",()=>main]]);_092‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_092‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);


       async function askForNoteContent(options) {
    const answer = await inquirer.prompt({
        type: 'input',
        name: 'content',
        message: "Please write the content of the note: ",
    });
    return answer.content;
}

       async function writeContent(content, name) {
    var fs = require('fs');
    // var oldContent = fs.readFileSync("./notes/" + name + ".txt", 'utf8');
    // console.log(oldContent);
    fs.writeFileSync("./notes/" + name + ".txt", content);
}

       async function checkContent(name) {
    var fs = require('fs');
    try {
        var oldContent = fs.readFileSync("./notes/" + name + ".txt", 'utf8');
        _092‍.g.console.log(oldContent);
    } catch(e) {
        _092‍.g.console.log(chalk.red.bold("ERROR 404"));
        process.exit(1);
    }
    
}

       async function continueContent(content, name, options) {
    var fs = require('fs');
    try {
        var oldContent = fs.readFileSync("./notes/" + name + ".txt", 'utf8');
        _092‍.g.console.log("This is the old content: \n" + oldContent);
        var newContent = await askForNoteContent(options);
        fs.writeFileSync("./notes/" + name + ".txt", newContent);
    } catch(e) {
        _092‍.g.console.log(chalk.red.bold("ERROR 404"));
        process.exit(1);
    }
    
}

       async function main(options) {
    if(options.create) {
        var content = await askForNoteContent(options);
        await writeContent(content, options.name);
    } else {
        if(options.check) {
            await checkContent(options.name);
        } else {
            if(options.continue) {
                await continueContent(content, options.name, options);
            }
        }
    }
    _092‍.g.console.log(chalk.green.bold("DONE"));
}

